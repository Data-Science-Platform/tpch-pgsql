language: python

python:
  - "3.4"

before_install:
  - python --version
  - pip --version
  - pg_config --version
  - psql --version

install:
  - pip install -r requirements.txt

script:
  - ./benchmark.py

